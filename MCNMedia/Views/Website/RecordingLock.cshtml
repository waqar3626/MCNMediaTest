@model MCNMedia_Dev.Models.GenericModel
@{
    ViewData["Title"] = "RecordingLock";
    Layout = "~/Views/Shared/_LayoutWebsite.cshtml";
}


@{
    TempData.Keep("RecordingPassword");
    TempData.Keep("RecordingId");
}


<div class="container-scroller">
    <div class="container-fluid page-body-wrapper full-page-wrapper">
        <div class="content-wrapper d-flex align-items-center auth lock-full-bg">
            <div class="row w-100">
                <div class="col-lg-4 mx-auto">
                    <div class="auth-form-transparent text-left p-5 text-center">
                        @*<label>@Model.Churches.ChurchName</label>*@
                        <form class="pt-5" asp-action="RecordingLock" asp-controller="Website" method="post">
                            <div class="form-group">
                                @*<input type="hidden" asp-for="recordingLock.RecordingPass" />*@
                                <input type="hidden" value=@ViewData["RecordingPassword"] name="RecordingPass" id="RecordingPassword" />
                                <input type="hidden" value=@ViewData["RecordingId"] name="recodingId" id="recodingId" />
                                <label for="examplePassword1">Recording Password</label>

                                <input type="password" asp-for="recordingLock.Password" class="form-control text-center" id="examplePassword1" placeholder="Password" name="Password">
                            </div>
                            <div class="mt-5">
                                <button type="submit" class="btn btn-block btn-pink text-white btn-lg font-weight-medium" href="@Url.Action("ChurchLock", "Website")">Unlock</button>
                            </div>
                            <br />
                          
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
